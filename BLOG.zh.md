# 超越文字：为什么 AI Agent 需要交互式 UI

**[English Version](./BLOG.md)**

*发布日期：2026-05-15*

---

我们都经历过。你让 AI Agent 算个东西，它回复：

```
99.5 × 3 = 298.5
```

技术上完全正确。但现在你想看看 4 件是多少。或者 5 件。或者加 8% 的税。每一次变化都意味着又一轮对话——描述你想改什么，等回复，再描述下一个变化。

**问题不是准确性。问题是媒介。**

聊天气泡里的数字是一个死胡同。一个可以摆弄的计算器才是一个起点。

## 核心洞察：UI > 文字（对于可交互的结果）

当 Agent 计算一个结果、生成一张图表、或创建一个表单时，把它展示为**一个可操作的 UI**，本质上比用文字描述它更好：

- 一个可以调整的计算器 > 消息里的一个数字
- 一个可以缩放的图表 > 对数据趋势的文字描述
- 一个可以填写的表单 > markdown 里的字段列表
- 一个可以调整的调色板 > 代码块里的十六进制色值

这不是"减少纠正错误"。这是**给用户一个可以思考的工具**。交互式 UI 让用户探索、实验、发现——这些是纯文字做不到的。

## 引入 ERI — Embedded Result Interface

ERI 是一种极其简单的模式，让 AI Agent 的输出变成可交互的。整个想法就是：

1. Agent 处理用户请求
2. Agent 调用第三方 API
3. Agent 嵌入第三方的交互页面（通过 iframe 或截图）
4. 用户直接在 UI 上交互

就这样。不需要新协议。不需要新标准组织。不需要安装 SDK。只是 Skill 定义的一种约定写法。

### 有多简单？

一个完整的 ERI Skill 定义只需要几行：

```markdown
---
name: notecalc
description: 数学计算器，支持变量和单位转换。
---

## 工作流
1. 将用户请求转化为表达式
2. 调用 POST /api/calculate 传入表达式
3. 以 iframe 嵌入结果: https://app.com/embed#encoded_expr
```

**10 分钟写完。半天做好嵌入页面。零平台改动。**

## 渐进增强

ERI 不要求你一步到位。它是渐进式的：

| 等级 | 你得到什么 | 工作量 |
|------|----------|--------|
| 0 | 纯文本（现状） | 无 |
| 1 | 交互 UI 的截图 | 加一个截图服务 URL |
| 2 | 用户可直接交互的 iframe | 平台支持 iframe |
| 3 | 通过 postMessage 双向通信 | 加一个消息处理器 |

从 Level 1 开始。上线。按需增强。不会破坏任何东西。

## 真实案例：NoteCalc

[NoteCalc](https://tsfullstack.heartstack.space/noteCalc) 是一个交互式计算笔记本。它支持变量、单位转换、等式验证——在一个实时编辑器中，输入即得结果。

通过 ERI，当用户告诉 Agent：

> "帮我算 3 件商品，每件 99.5 元"

Agent 不会只说 `298.5`。它会嵌入 NoteCalc：

```
┌──────────────────────────────────────┐
│  NoteCalc                             │
│                                       │
│  价格 = 99.5            → 99.5        │
│  总价 = 价格 * 3         → 298.5       │
│                                       │
│  [用户可以直接编辑数值]                   │
└──────────────────────────────────────┘
```

用户可以：
- 把 `3` 改成 `4` → 结果即时变为 `398`
- 加一行 `税 = 总价 * 0.08` → 看到 `23.88`
- 转换单位：`总价 to cents` → `29850`
- 全程不需要再对 Agent 说一个字

当他们继续对话（"加 15 元运费"），Agent 读取上下文并输出一个**新的**嵌入式计算器。

## 为什么这样行得通

**对 Agent 平台**：无需协议变更。如果能渲染 iframe（大多数已经可以），就支持 ERI。

**对第三方应用**：把现有 UI 暴露为可嵌入页面即可。大多数 Web 应用半天就能做到。不需要学习任何 Agent 专用 API。

**对 Skill 作者**：写一个 `skill.md`。就这样。没有 SDK、没有库、没有构建步骤。

**对用户**：他们得到一个可以实际使用的东西，而不只是阅读。AI 处理重活（理解意图、调用 API），用户处理微调（编辑、探索、调整）。

## 对比

| 方案 | 做什么 | 接入成本 |
|------|--------|---------|
| **MCP** | 让 Agent 调用工具 | 1-2 天 |
| **A2UI** | AI 动态生成 UI | 3-5 天 |
| **Function Calling** | LLM 调用函数 | 1 天 |
| **ERI** | 让工具结果可交互 | 10 分钟 |

ERI 不与这些方案竞争——它叠加在上面。用 MCP 调 API，用 ERI 交互式地展示结果。

## 亲自试试

访问 [在线演示](https://eri-spec.github.io/demo/)——一个极简的 Agent UI，你可以看到 ERI 与 NoteCalc 配合的效果。

演示完全在你的浏览器中运行。你提供自己的 LLM API Key（它永远不会离开你的浏览器——没有服务器参与）。

## 更大的图景

我们正在进入一个 AI Agent 将成为许多任务主要界面的时代。但今天 Agent 的输出主要是文字。对于总结和分析，这没问题。对于用户需要*交互*的场景，这不够。

ERI 是让 Agent 输出更像软件、更不像邮件的一小步。不是每个 Agent 回复都需要嵌入式 UI。但对于需要的那些，体验是天壤之别。

---

*ERI 是一个开放模式。无需许可，无需授权。写一个 Skill，嵌入一个 URL。[完整规范](./SPEC.zh.md) 在本仓库中。*
