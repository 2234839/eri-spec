# 超越文字：为什么 AI Agent 需要交互式 UI

**[English Version](./BLOG.md)**

*发布日期：2026-05-15*

---

## 文字陷阱

今天的 AI Agent 都有同样的问题。你让它算个东西，它回复：

```
99.5 × 3 = 298.5
```

对的。但你想看 4 件是多少。或者 5 件。或者加 8% 的税。每次变化都意味着又一轮对话——描述想改什么，等回复，再描述下一个。

问题不是准确性。问题是媒介。

不只是计算。Agent 生成图表时，你想调坐标轴。创建表单时，你想改字段。选颜色时，你想拖滑块。每次 Agent 都把你逼回文字——而你需要的明明是一个 UI。

Agent 生态解决了输入——自然语言、工具调用、多模态。没解决输出。结果都回到文字。

## 引入 ERI

ERI（Embedded Result Interface）是一种让 Agent 输出可交互的约定。Agent 调用第三方 API，构造嵌入 URL，输出 iframe 或截图。用户直接交互——Agent 不在循环中。

不需要新协议。不需要 SDK。只需要一个 `skill.md`。

```markdown
---
name: notecalc
description: 数学计算器，支持变量和单位转换。
---

## 工作流
1. 将用户请求转化为表达式
2. 调用 POST /api/calculate 传入表达式
3. 以 iframe 嵌入结果: https://app.com/embed#encoded_expr
```

10 分钟写完。半天做好嵌入页面。零平台改动。[Level 1（截图）](./SPEC.zh.md)现在就能用——不需要平台支持。上线，准备好时升级到 Level 2（iframe）。不会破坏任何东西。

试一下：[2234839.github.io/eri-spec/](https://2234839.github.io/eri-spec/)

## ERI vs A2UI

两条路径到交互式 Agent 输出：

| | ERI | A2UI (Google) |
|---|---|---|
| **路径** | 通过 URL 嵌入已有应用 | Agent 以 JSON 声明 UI，客户端渲染 |
| **Agent 需要** | 一个 `skill.md` | SDK + Schema 定义 |
| **平台需要** | 渲染 iframe（已经有了） | A2UI 渲染器 + 组件目录 |
| **第三方需要** | 一个 HTTPS 嵌入页面 | 组件目录 + A2UI Schema |
| **成本** | 10 分钟 | 数天 |

A2UI 对构建自己渲染管线的平台来说是对的。但需要数天集成和平台配合，才能在屏幕上出现任何东西。

ERI 更快到达。一个已有 Web UI 的应用，半天就能出现在 Agent 对话中。没有 SDK、没有 Schema、没有渲染器。一个 URL。两条路收敛到同一个终点。

```
┌─────────────────────────────────────────────┐
│              Agent 输出层                     │
│                                             │
│   Level 2: 用户可直接交互                     │
│   ┌─────────────┐    ┌──────────────────┐  │
│   │     ERI     │    │      A2UI        │  │
│   │  嵌入 URL   │    │  JSON Schema +   │  │
│   │  → iframe   │    │  渲染器          │  │
│   └─────────────┘    └──────────────────┘  │
│                                             │
│   Level 1: 截图（ERI 兜底）                   │
│   Level 0: 纯文本（现状）                     │
├─────────────────────────────────────────────┤
│              工具调用层                       │
│   MCP  ·  Function Calling  ·  REST        │
└─────────────────────────────────────────────┘
```

---

*ERI 基于 MIT 许可。读[规范](./SPEC.zh.md)，写一个 Skill，嵌入一个 URL。*
